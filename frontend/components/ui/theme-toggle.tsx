"use client"

import * as React from "react"

// ThemeToggle removed: site is permanently dark with no toggle. Keep a noop export
export function ThemeToggle(_props: { className?: string }) {
  return null
}
