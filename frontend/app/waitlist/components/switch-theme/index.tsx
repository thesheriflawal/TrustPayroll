'use client'

// ThemeSwitcher removed: site is permanently dark with no toggle. Export a noop
import * as React from 'react'

export function ThemeSwitcher(_props: { className?: string }) {
  // Keep API stable for imports; render nothing.
  return null
}
